CREATE TABLE [dbo].[Table]
(
	[Id] INT NOT NULL PRIMARY KEY IDENTITY, 
    [Date] DATE NOT NULL, 
    [TL] DECIMAL(18, 4) NOT NULL, 
    [USD] DECIMAL(18, 4) NOT NULL, 
    [EUR] DECIMAL(18, 4) NOT NULL
)
